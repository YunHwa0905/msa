spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # ?? ????? ????!!
        - id: service1-route
          uri: lb://SERVER-1
          # URL ??, ??? ?? ??, ???,....
          predicates:
            - Path=/service1/**

        - id: service2-route
          uri: lb://SERVER-2
          # URL ??, ??? ?? ??, ???,....
          predicates:
            - Path=/service2/**

# ????? ??, ?? ??(?? ?????)? ? ??? ?? -> 8080
server:
  port: 8080

# ??? ?? ??
eureka:
  client:
    service-url:
      # ??? ??? ??? 404?
      defualtZone: http://localhost:8761/eureka